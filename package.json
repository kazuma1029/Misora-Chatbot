{
  "name": "ai-cloud-work",
  "private": true,
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix web\" \"npm run dev --prefix server\"",
    "install:all": "npm install && npm install --prefix web && npm install --prefix server",
    "build": "npm run build --prefix web && npm run build --prefix server",
    "check": "npm run check --prefix web && npm run check --prefix server"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.18.0",
    "@aws-amplify/backend-cli": "^1.8.0",
    "aws-cdk-lib": "^2.216.0",
    "concurrently": "9.2.1",
    "constructs": "^10.4.3",
    "esbuild": "^0.27.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "aws-amplify": "^6.15.8"
  }
}
